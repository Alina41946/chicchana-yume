---
import { getCollection } from "astro:content";

import Layout from "../../../layouts/Layout.astro";
import ArticleList from "../../../components/ArticleList.astro";

const posts = await getCollection(
  "blog",
  ({ data }) =>
    !data.draft &&
    data.category === "language" &&
    data.subcategory === "japanese"
);

const japanesePosts = posts.sort(
  (a, b) =>
    b.data.publishedDate.getTime() -
    a.data.publishedDate.getTime()
);

const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;

---

<Layout
  title="Japanese｜ちっちゃな私には、ちっちゃな夢があります。"
  description="JLPT、教材、自學方法與日文學習紀錄。"
>
  <main class="japanese-page">

    <section class="page-hero">
      <div class="container">

        <nav
          class="breadcrumb"
          aria-label="麵包屑導覽"
        >
          <ol>
            <li>
              <a href={base}>HOME</a>
            </li>

            <li>
              <span aria-hidden="true">/</span>
              <a href={`${base}language/`}>
                LANGUAGE
              </a>
            </li>

            <li>
              <span aria-hidden="true">/</span>
              <span aria-current="page">
                JAPANESE
              </span>
            </li>
          </ol>
        </nav>

        <p class="page-eyebrow">
          LANGUAGE / JAPANESE
        </p>

        <h1>日本語の記録</h1>

        <p class="page-description">
          JLPT、自學教材、讀書方法，以及一路累積下來的日文學習紀錄。
        </p>

      </div>
    </section>

    <section class="section">
      <div class="container">

        <div class="section-header">
          <div>
            <p class="section-eyebrow">
              STORIES
            </p>

            <h2>Japanese Journal</h2>
          </div>

          <span class="post-count">
            {japanesePosts.length} STORIES
          </span>
        </div>

        <ArticleList
  posts={japanesePosts}
  categoryLabel="日文學習"
  accent="var(--color-language)"
  emptyMessage="目前還沒有日文文章。"
/>

      </div>
    </section>

  </main>
</Layout>

<style>
  .page-hero {
    padding-block: var(--space-7);

    border-bottom: 1px solid
      var(--color-border);
  }

  .breadcrumb {
    margin-bottom: var(--space-4);
  }

  .breadcrumb ol {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-1);

    color: var(--color-text-muted);

    font-size: 0.75rem;
    letter-spacing: 0.06em;
  }

  .breadcrumb li {
    display: flex;
    align-items: center;
    gap: var(--space-1);
  }

  .breadcrumb a {
    transition:
      color var(--transition-base);
  }

  .breadcrumb a:hover {
    color: var(--color-language);
  }

  .page-eyebrow,
  .section-eyebrow {
    margin-bottom: var(--space-1);

    color: var(--color-text-muted);

    font-size: 0.75rem;
    letter-spacing: 0.15em;
  }

  .page-hero h1 {
    margin-bottom: var(--space-2);
  }

  .page-description {
    max-width: 560px;
    margin-bottom: 0;

    color: var(--color-text-muted);
  }

  .section-header {
    margin-bottom: var(--space-4);

    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: var(--space-3);
  }

  .section-header h2 {
    margin-bottom: 0;
  }

  .post-count {
    color: var(--color-text-muted);

    font-size: 0.7rem;
    letter-spacing: 0.1em;
  }


  @media (max-width: 767px) {
    .page-hero {
      padding-block: var(--space-5);
    }

    .breadcrumb {
      margin-bottom: var(--space-3);
    }

    .section-header {
      align-items: start;
      flex-direction: column;
      gap: var(--space-1);
    }

    
  }
</style>